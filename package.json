{
  "name": "skyway-tutorial",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel doc/index.html --open",
    "dev": "rm -rf build && parcel build doc/index.html --dist-dir build --public-url /skyway-tutorial/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@skyway-sdk/room": "^1.7.1"
  },
  "devDependencies": {
    "buffer": "^6.0.3",
    "parcel": "^2.12.0"
  }
}
